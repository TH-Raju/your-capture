import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Joined",
  description: "Your Capture Challengers Joined page",
};

const JoinedPages = () => {
  return <div>Joined Pages</div>;
};

export default JoinedPages;
